### Vagrant test box

* Install Vagrant
* Install Virtualbox
* Install Vagrant plugin: `vagrant plugin install vagrant-vbguest`
* Run `vagrant up`

```bash
cd vagrant
vagrant plugin install vagrant-vbguest
vagrant up
```
